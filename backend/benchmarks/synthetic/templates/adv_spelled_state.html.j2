<!DOCTYPE html>
<!-- Adversarial: spelled-out US state name in location. parser.py:107 only
     accepts 2-letter abbreviations against `us.states` set. "Austin, Texas"
     should still resolve to country=United States but won't. -->
<html><head><meta charset="utf-8"><style>{{ css }}</style></head><body>
<h1>{{ profile.name }}</h1>
<div class="contact">{{ profile.email }} | {{ profile.phone }} | {{ profile.location.raw }}</div>

<h2>Skills</h2>
<ul class="skills-list">
{% for s in rendered_skills %}<li>{{ s }}</li>
{% endfor %}</ul>

<h2>Experience</h2>
{% for e in profile.experience %}
<div class="experience-entry">
  <div class="entry-header">{{ e.title }} — {{ e.company }}</div>
  <div class="entry-meta">{{ e.location_raw }} | {{ e.dates }}</div>
</div>
{% endfor %}
</body></html>
